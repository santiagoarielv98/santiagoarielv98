/* import React from "react"; */

/* import { ThemeContext } from "../contexts/ThemeContext"; */

/**
 * Hook que se encarga de manejar el tema de la aplicación
 * @returns theme: el tema actual de la aplicación, themeMode: el modo de tema actual, toggleTheme: función para cambiar el tema
 */
export function useTheme() {
  /* const context = React.useContext(ThemeContext);

  if (!context) {
    throw new Error("useTheme must be used within a ThemeProvider");
  }

  return context; */
}
